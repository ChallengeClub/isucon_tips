参加者：ヒビキ、ひでたん、ともさん

## 本日やったこと
- ペアプロ練習（ヒビキ、ひでたん）
- ISUCON13攻略の足跡の確認と、MySQLの攻略を一歩。

## 議論
- IceBreak：調子はいかが？
  - チーム巻込みとかAstraとかゲーム制作とかPC(RTX2070)とかVRとか
- 今日は何しましょう？
  - ISUCON13攻略、Rustの構造設計にトライ中。
  - ペアプロ＆何らかのチューニング、実施してみましょう！

## ペアプロ練習 
- ペアプロ＝ドライバー：ヒビキさん、ナビゲータ：ひでたん
- ISUCON13攻略で、AWS上で動いているSQLを触ってみる。
  ```
  $ mysql -u isucon -p                                               # MySQLに接続する。パスワードはisucon。
  mysql> show databases;                                             # DB一覧...ISUPIPEやISUDNSが有る。
  mysql> use ISUPIPE;                                                # DB接続...今日はISUPIPEを調べる。
  mysql> show tables;                                                # TABLE一覧...livestreamsやlivecommentsが重要そう。
  mysql> describe livecomments;                                      # TABLE構造が判る。idのみKey(Index)がある。
  mysql> SHOW TABLE STATUS FROM ISUPIPE LIKE livecomments;           # TABLE状態が判る。1000行くらいあった。
  mysql> select * from livecomments limit 10;                        # コメントを10件表示する。
  mysql> select * from livecomment where livestream_id = 10 limit 10;
                                                # 指定したlivestream_idのコメントを10件表示する。
  mysql> explain select * from livecomment where livestream_id = 10 limit 10;
                                                # クエリの実行計画を表示する。Key=NULLで、1000件全部を検索。遅そう。
  mysql> select * from livecomment where id = 10 limit 10;          # 指定したidのコメントを10件表示する。
  mysql> explain select * from livecomment where id = 10 limit 10;
                                                # クエリの実行計画を表示する。使えるKey(Index)があって1件でヒットする。速そう。
  ```
- 論よりexplain.
- 完全に理解したので、本日はここまで！
