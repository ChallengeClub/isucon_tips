参加者：アワノ、モリアイ、nuno8、ひでたん

## 本日やったこと
- ペアプロ練習（ひでたん、アワノ）
- SQLの攻略を一歩。

## 議論
- IceBreak：会社でLT回（巡回セールスマン問題シリーズ！！）＠アワノさん、IPA高度試験に挑戦＠モリアイ、ISUCON本読解＠nuno8、仕事頑張ってた！@ヒビキ、メントスコーラ＠ひでたん
- 本日の素振り攻略
  - bench -> CICD -> benchしてパフォーマンスアップ をまず試したい！
  - パフォーマンスの可視化まで思い出したい！
  - ソリューションの選択肢は色々！（pproteinとか、grafanaとか、openteremetry経由）
  - grafanaでframegraph表示も出来そう。
  - pproteinはISUCON特化の計測ツール。（紹介、リポジトリ）ただしやる場合には事前に準備と練習が必要！
  - ssh鍵生成とgithubへの設定は初期に必要（←あとからでOK）
  - 同じ共同作業用のAWS(isupipe)にみんなで入るのもやってみたい

## ペアプロ
- ひでたんさんのAWS環境でprivate isuのSQL改善に挑戦
  - ドライバー　ひでたんさん、　ナビゲーター　アワノさん、　ロガー　ヒビキ
- まずはipをlocalhostに指定してベンチマークを実行，score 339点
- SQL改善、まずはスロークエリログを確認するところだが、今回はデータベースの中身を調査
- アワノさん，前回のISUCONではDBeaverを使用してGUIでSQLを使用していた
- topコマンドからMysqlのポートを確認　3306 33060どちらかの模様
- ファイアウォールのポートを開放し、外部からDBeaverがアクセスできるように変更
- Mysqlのログイン、Private isuのためパスワードがわからない、過去のログ等から調査
  - id:isucon pass:isuconではだめだった、id:isuconp pass:isuconpだった...紛らわしい 
- 一度CLIからdata baseにアクセスに切り替え
- mysql -u isuconp -pでMySqlにアクセス
  - show database;でisuconpというdatabaseを確認
　- cibbect isuconp;で接続
　- show tabels;でtableを確認
  - explain users;でuser tableを確認
- DBeaverを使用したらSQLのデータ一覧を確認可能
  - /etc/mysql/mysal.conf.d/mysqld.cnfを編集しなければDBeaverには接続できない
  - viで編集しようとしたらreadonlyだったため権限エラー、sudoで解決
  - bind-addrressという項目をコメントアウトする必要がある
  - sudo systemctl restart mysqlでmysqlを再起動
  - sudo systemctl sutatus mysqlで起動確認，無事起動
  - DBeaverに接続はできたが、権限関係でエラー
  - アワノさんの感覚としてDBeaverを使えていたら、もっと楽に高得点を狙えたかもしれないとのこと
  - /etc/mysql/conf.d/mysql.conf.d.cnfを確認したがなにも書かれておらずわからない
  - DBeaverの接続はいったん置いておく
- 教科書 p124参照
- SET GLOBAL slow_query_log = 1;でスロークエリを設定
- SET GLOBAL slow_query_log_file = "/var/log/mysql/mysql-slow.log";で出力先を設定
- sudo apt update
- sudo apt install pt-query-digest
- pt-query-digest --vでインストールを確認
- ベンチマークを実行してデータベースの流れを確認 スコアは354点
- pt-query-digest /var/log/mysql/mysql-slow.log パーミッション関係で動かず
- sudoで実行、だが#No event processd.でなにもlogが書き込まれていない
- SET GLOBAL slow_query_log = 1;は一時的な設定
- /etc/mysql/mysal.conf.d/mysqld.cnfのslow_quertlogのコメントアウトを解除
- long_query_time を 0に変更
- 設定ファイルを変更したのでmysqlのリスタート
- 再度ベンチを実行
- 実行中にlogをtailで覗いてみる
- 無事ログ出力を確認！
- Rows_examindが100002など明らかに重そうなものを発見
- select count(*) from comments;で確認100000
- 先ほどのログと近い値、commentsを検索している人たちが多くいそう
- /var/log/mysql/mysql-slow.logを出力
- 処理の重たいランキングで表示されている
- post-idで検索しているのが原因で重たくなっているらしい
- explain select * from comments where post_id = 5162 limit 1;を実行rowsが99917もほぼすべてのrowを参照している
- keyの張り直し
- alter table 'comments' add index 'idx_post_id' ('post_id');を実行シンタックスエラー
- 何と'(シングルコーテーション)ではなく`(バックコーテーション)だった！
- 最後にもう一度ベンチマークの実行
  - keyを一つ変えるだけでcpu使用率が90から45ほどに
  - スコアはなんと8084点まで上昇！
